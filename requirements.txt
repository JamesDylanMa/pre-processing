# Core dependencies
streamlit>=1.28.0
pandas>=2.0.0
numpy>=1.24.0

# PyTorch GPU support (CUDA 11.8 or 12.1)
# For CUDA 11.8: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
# For CUDA 12.1: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# CPU only: pip install torch torchvision torchaudio
torch>=2.0.0  # PyTorch (GPU support if CUDA is available)
torchvision>=0.15.0
torchaudio>=2.0.0

# Document parsing libraries
PyPDF2>=3.0.0
pypdf>=3.0.0  # Modern successor to PyPDF2
pdfplumber>=0.10.0
PyMuPDF>=1.23.0  # Fast and accurate PDF parser (fitz)
pdfminer.six>=20221105  # Good for text extraction with layout info

# Optional: Table extraction (requires Java/OpenCV)
# camelot-py[cv]>=0.11.0  # Table extraction from PDFs (requires Java and OpenCV)
# tabula-py>=2.5.0  # Table extraction from PDFs (requires Java)

# Optional: OCR for scanned PDFs
pdf2image>=1.16.0  # Convert PDF to images for OCR
pytesseract>=0.3.10  # OCR engine wrapper (requires Tesseract installation)
easyocr>=1.7.0  # Better OCR alternative, supports 80+ languages (no external dependencies)
Pillow>=10.0.0  # Image processing for OCR

# Optional: Advanced document processing
unstructured[pdf]>=0.10.0  # Extract structured data from unstructured documents
pdfquery>=0.4.3  # PDF data extraction using CSS-like selectors

python-docx>=1.0.0
openpyxl>=3.1.0
python-pptx>=0.6.21
xlrd>=2.0.1

# HTTP requests for Ollama
requests>=2.31.0

# Utilities
python-multipart>=0.0.6
pathlib2>=2.3.7

# Optional: For enhanced PDF processing
# pdfminer.six>=20221105
# pymupdf>=1.23.0

# Optional: For image processing
# Pillow>=10.0.0
# opencv-python>=4.8.0


